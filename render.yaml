services:
  - type: web
    name: gameforgehub
    plan: free
    runtime: native
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/*.jar
    envVars:
      - key: JAVA_VERSION
        value: 21
      - key: PORT
        value: 8080